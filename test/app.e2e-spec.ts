/**
 * E2E contra API real: usar con PostgreSQL levantado
 * (p. ej. docker compose up -d) y DATABASE_URL en .env
 */
describe('e2e (placeholder)', () => {
  it('añade pruebas e2e con supertest cuando haya BD en CI', () => {
    expect(true).toBe(true);
  });
});
